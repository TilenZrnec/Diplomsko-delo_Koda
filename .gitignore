__pycache__/
*.pyc
venv/
.venv/
*.egg-info/
.ipynb_checkpoints/

data/openml_cache/
# Medic3 so zaupni medicinski podatki (anonimizirani in zamegljeni) in so
# hkrati veliki 142 MB - cez 100 MB omejitev GitHuba. V repozitorij NE gredo;
# izvor in kontrolna vsota sta zabelezena v results/medic3/PROVENANCE.md.
data/medic3/
catboost_info/
logs/
.cache/
*.sif
results/per_dataset/

# Zagoni benchmarka (results/runs/<run_id>/): napovedi so velike in izpeljane,
# nedokončani partiali pa začasni. Kurirani CSV-ji in manifest.json se commitajo.
results/runs/*/predictions/
results/runs/*/per_dataset/*.partial
results/runs/*/per_dataset/*.tmp
